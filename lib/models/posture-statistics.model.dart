class PostureStatistics {
  final String posture;
  final double value;
  final DateTime startTime;
  final DateTime endTime;

  PostureStatistics(this.posture, this.value, this.startTime, this.endTime);
}
